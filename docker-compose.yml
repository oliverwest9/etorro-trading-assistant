services:
  surrealdb:
    image: surrealdb/surrealdb:v2.3.6
    user: root
    command: start --user ${SURREAL_USER:-root} --pass ${SURREAL_PASS:-root} surrealkv:/data/trading.db
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - surrealdb_data:/data

volumes:
  surrealdb_data:
